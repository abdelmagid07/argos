"""Kafka producer / consumer for streaming transactions into ``raw_transactions``.

From the repo root:
    python -m src.kafka_ingest.producer
    python -m src.kafka_ingest.consumer

Environment variables: see ``.env.example`` (``KAFKA_*``).
"""
