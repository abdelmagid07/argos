# Offline store (Stage 2)
DATABASE_URL=

# Online store — Redis (Stage 3)
REDIS_URL=

# Streaming ingest — Kafka (Stage 4)
# After `docker compose up -d zookeeper kafka`
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC=transactions
KAFKA_DLQ_TOPIC=transactions-dlq
KAFKA_GROUP_ID=postgres-sink
